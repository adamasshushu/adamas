{
  "name": "adamas",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/tools",
    "packages/gateway",
    "packages/cli"
  ],
  "scripts": {
    "build:native": "cd packages/native/terminal && cargo build --release",
    "build": "tsc -b",
    "start": "npx tsx packages/cli/index.ts",
    "adamas": "npx tsx packages/cli/index.ts",
    "test": "echo 'tests coming'"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "tsx": "^4.0",
    "@types/node": "^20"
  }
}
